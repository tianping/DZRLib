% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_decide_sex.R
\name{seurat_decide_sex}
\alias{seurat_decide_sex}
\title{Predict sample sex based on marker gene expression in scRNA-seq data}
\usage{
seurat_decide_sex(
  object,
  group.by = "orig.ident",
  male_markers = c("RPS4Y1", "RPS4Y2", "DDX3Y"),
  female_markers = c("XIST", "LOC106995245"),
  assay = "RNA",
  layer = "counts",
  heatmap_log = TRUE,
  heatmap_scale_by_gene = TRUE,
  scatter_log = TRUE,
  sex_ratio_threshold = 2,
  output_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object}

\item{group.by}{Character string specifying the metadata column that identifies
individual samples. Default: "orig.ident"}

\item{male_markers}{Character vector of male marker genes.
Default: c("RPS4Y1", "RPS4Y2", "DDX3Y")}

\item{female_markers}{Character vector of female marker genes.
Default: c("XIST", "LOC106995245")}

\item{assay}{Character string specifying which assay to use. Default: "RNA"}

\item{layer}{Character string specifying which layer to use (for Seurat v5).
Default: "counts"}

\item{heatmap_log}{Logical; whether to use log2(CPM+1) for heatmap. Default: TRUE}

\item{heatmap_scale_by_gene}{Logical; whether to scale by row (gene) in heatmap.
Default: TRUE}

\item{scatter_log}{Logical; whether to use log2(CPM+1) for scatter plot. Default: TRUE}

\item{sex_ratio_threshold}{Numeric; threshold for male/female expression ratio.
Samples with ratio > threshold are classified as male, < 1/threshold as female.
Default: 2}

\item{output_dir}{Character string; directory to save output files. If NULL,
no files are saved. Default: NULL}

\item{verbose}{Logical; whether to print progress messages. Default: TRUE}
}
\value{
A list containing:
\item{metadata}{Data frame with sample metadata, marker gene CPM values, and PredictedSex}
\item{pseudo_bulk_cpm}{Matrix of CPM values for marker genes (pooled by sample)}
\item{scatter_plot}{ggplot object showing average male vs female marker expression}
\item{heatmap}{ggplot object showing expression of all marker genes across samples}
\item{predicted_sex}{Named character vector of predicted sex for each sample}
}
\description{
This function pools single-cell data by individual, calculates CPM values,
and uses male/female marker genes to predict the sex of each sample.
}
\examples{
\dontrun{
result <- seurat_decide_sex(
  object = seurat_obj,
  group.by = "donor_id",
  output_dir = "results/sex_prediction"
)
}

}
